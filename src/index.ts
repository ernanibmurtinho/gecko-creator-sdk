export { createVault } from "./vault"
export type { GeckoConfig, VaultParams, VaultResult, GeckoVaultProps } from "./types"

// Note: GeckoVault React component will be added in Sprint 5
// It requires @privy-io/react-auth peer dependency for signing
